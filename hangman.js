// constructor function which can take in a series of values and create objects
// with the properties contained inside
function Words(location, phrase, letter) {
  this.location = location;
  this.phrase = phrase;
  this.letter = letter;
